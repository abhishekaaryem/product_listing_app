import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const primary = Color(0xFF6C63FF);
  static const background = Colors.white;

  // Text Colors
  static const textPrimary = Color(0xFF000000);
  static const textSecondary = Color(0xFF4E4D4D);
  static const textTertiary = Color(0xFF646363);

  // Input Colors
  static const inputBorder = Color(0xFFE0E0E0);
  static const inputFocused = Colors.blue;

  // Button Colors
  static const buttonPrimary = Color(0xFF6C63FF);
  static const buttonText = Colors.white;
}
